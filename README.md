# Kanban Management System (Laravel)

Современная канбан‑система, построенная на Laravel, предназначенная для управления задачами, заказами, клиентами и рабочими процессами. Проект включает гибкую архитектуру, поддержку нескольких досок, вложений, комментариев, уведомлений и интеграций.

---

## 🚀 Возможности

### 📌 Управление досками
- Создание досок с уникальным UUID
- Получение и обновление досок
- Экспорт данных доски
- Управление тегами внутри доски

### 📌 Колонки (Threads)
- Создание колонок внутри доски
- Переименование и удаление колонок
- Переупорядочивание колонок
- Получение задач внутри колонки (с пагинацией)

### 📌 Задачи
- Создание задач разных типов (Base, User, Order, Text, Finance)
- Обновление и удаление задач
- Перемещение задач между колонками
- Дублирование задач
- Отметка задачи как просмотренной
- Поддержка вложений (файлы, изображения, аудио, видео)
- Поддержка комментариев с файлами

### 📌 Комментарии
- Добавление комментариев к задаче
- Прикрепление файлов к комментариям
- Получение списка комментариев

### 📌 Вложения
- Загрузка файлов в задачу
- Получение списка вложений
- Поддержка любых типов файлов

### 📌 Push‑уведомления
- Подписка на уведомления
- Тестовая отправка уведомлений

### 📌 Внешний API
Универсальный метод для создания задач через HTTP‑запросы:
- принимает `board_uuid`
- принимает `thread` (номер колонки)
- автоматически подбирает колонку
- подставляет дефолтные данные по типу задачи

---

## 🧱 Архитектура

Проект разделён на модули:
- **Boards** — управление досками и тегами
- **Columns** — управление колонками и порядком
- **Tasks** — создание, обновление, перемещение, вложения
- **Comments** — комментарии и файлы внутри комментариев
- **External API** — единая точка входа для интеграций
- **Push** — уведомления браузеру

Каждый модуль имеет собственные контроллеры и маршруты, сгруппированные по префиксам.

---

## 📡 Основные маршруты API

### Boards
GET    /api/boards/{uuid}
PUT    /api/boards/{uuid}
POST   /api/boards
GET    /api/boards/{board}/export
GET    /api/boards/{uuid}/tags
POST   /api/boards/{uuid}/tags
POST   /api/boards/{uuid}/columns
POST   /api/boards/{uuid}/tasks
PUT    /api/boards/{uuid}/columns/reorder

### Columns
PUT    /api/columns/{column}
DELETE /api/columns/{column}
GET    /api/columns/{column}/tasks
PUT    /api/columns/{column}/tasks/reorder

### Tasks
PUT    /api/tasks/{task}
DELETE /api/tasks/{task}
POST   /api/tasks/move
POST   /api/tasks/{task}/duplicate
POST   /api/tasks/{task}/view

### Comments
GET    /api/task/{task}/comments
POST   /api/task/{task}/comment

### Attachments
GET    /api/task/{task}/attachments
POST   /api/task/{task}/attachments

### External API
POST   /api/task/create

### Push
POST   /api/task/create

---

## 🛠 Установка и запуск

### 1. Установка зависимостей
composer install
npm install

### 2. Настройка окружения
Создать `.env`:
cp .env.example .env
php artisan key:generate

### 3. Миграции и сиды
php artisan migrate
php artisan db:seed

### 4. Запуск сервера
php artisan serve

---

## 🧩 Frontend

Фронтенд построен на:

- Vue 3
- Pinia
- Axios
- Компонентной архитектуре

Реализованы:

- список комментариев
- форма добавления комментария
- список вложений
- загрузка вложений
- предпросмотр изображений, аудио, видео

---

## 🎯 Для чего предназначена система

Этот проект — гибкая канбан‑платформа, которую можно использовать для:

- управления заказами
- учёта клиентов
- ведения внутренних задач
- работы торговых представителей
- визуализации бизнес‑процессов
- интеграции с Telegram mini‑apps
- построения CRM‑функционала

Система легко расширяется и подходит как для небольших команд, так и для сложных бизнес‑процессов.

